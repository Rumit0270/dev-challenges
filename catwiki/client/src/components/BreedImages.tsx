import React from 'react';

interface BreedImagesProps {
  images: string[];
}

const BreedImages: React.FC<BreedImagesProps> = ({ images }): JSX.Element => {
  return <div>BreedImages</div>;
};

export default BreedImages;
